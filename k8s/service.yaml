apiVersion: v1
kind: Service
metadata:
  name: fileshare-backend-service
spec:
  type: ClusterIP
  selector:
    app: fileshare-backend
  ports:
    - port: 8080
      targetPort: 8080
      name: http
